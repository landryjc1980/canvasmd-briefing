# Copy to .env.local. Both values are the PUBLISHABLE (anon) Supabase credentials —
# safe to expose; they only reach the public `briefing` edge function, which returns
# already-public content (clinicians' own X posts, published-podcast glosses, journal
# articles, FDA/regulatory events). No service-role key is used anywhere in this app.

SUPABASE_URL=https://zeqqxnaywtxofswujqub.supabase.co
SUPABASE_ANON_KEY=
